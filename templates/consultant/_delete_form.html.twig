<form method="post" action="{{ path('consultant_delete', {'id': consultant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consultant.id) }}">
    <p class="button-custom pricing-entry"><a href="" class="price py-2 px-3">Delete</a></p>
</form>